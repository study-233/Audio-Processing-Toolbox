# 音频处理工具箱设计文档

**Audio Processing Toolbox**

---

## 1. 项目概述

本工具箱是一个**模块化音频处理软件**，以“工具集合”为核心设计思想。
每个工具完成**单一、明确的音频处理任务**，支持常见音频格式，适用于语音处理与数字信号处理实验。

---

## 2. 功能模块划分

```text
Audio Toolbox
├── audio_io
├── audio_editor
├── audio_noise_remover
└── audio_format_converter
```

---

## 3. 功能设计说明

### 3.1 audio_io（音频输入输出）

**功能**

* 音频读取
* 音频保存

**说明**

* 作为所有工具的统一 I/O 接口
* 支持单声道与多声道音频

---

### 3.2 audio_changer（音频变换工具）

**功能**

* 变声（音高升高 / 降低）
* 语速调整（加速 / 减速，音高保持）
* 回声效果添加

**可调参数**

* 音高偏移（半音）
* 语速倍率
* 回声延迟时间
* 回声衰减系数


---

### 3.3 audio_editor（音频编辑工具）

**功能**

* 音频切分（按时间段）
* 去除语音中较长静音
* 去除语音首尾静音
* 设置采样率
* 设置通道数（单声道 / 双声道）

**说明**

* 支持批处理
* 静音检测基于短时能量阈值


---

### 3.5 audio_noise_remover（语音降噪）

**功能**

* 降低环境背景噪声
* 提升语音清晰度

**说明**

* 基于频谱减法
* 噪声强度可调

---

## 4. UI 设计原则

* 工具箱式结构，一次只使用一个工具
* 深色主题，专业音频软件风格
* 中央区域预留高级可视化（波形 / 标记 / 动态反馈）
* 参数区简洁、可控

---

## 5. UI 布局结构

```text
┌──────── 工具箱列表 ────────┐
│ audio_io                   │
│ audio_changer              │
│ audio_editor               │
│ format_converter           │
│ noise_remover              │
├──────── 主可视化区域 ──────┤
│ 波形 / 标记 / 状态显示     │
├──────── 参数控制区 ────────┤
│ 输入 | 参数 | 执行 | 输出  │
└──────── 状态栏 ───────────┘
```


